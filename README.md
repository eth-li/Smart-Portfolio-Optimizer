# Smart-Portfolio-Optimizer
User inputs current holdings + picks a risk bucket (Conservative/Moderate/Aggressive). Tool pulls historical returns + options-implied volatility, builds a covariance matrix, and recommends optimal weights. Output: efficient frontier chart showing current vs. optimized portfolio, plus per-stock allocation changes.
